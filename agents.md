# Instructions for coding agents

This is a CLI application written in Rust that allows to install
and execute third-party CLI applications.
Installation happens by downloading the official executables from places like
GitHub Releases and extracting the archives.
Installed applications are stored in a folder in the user's home directory
called the Yard.

To see the changes in a feature branch, run `git town diff-parent`.
